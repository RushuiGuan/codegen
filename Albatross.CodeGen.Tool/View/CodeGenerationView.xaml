<UserControl x:Class="Albatross.CodeGen.Tool.View.CodeGenerationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Albatross.CodeGen.Tool.View"
			 xmlns:vm="clr-namespace:Albatross.CodeGen.Tool.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<UserControl.Resources>
		<vm:DefaultDataTemplateSelector x:Key="templateSelector"/>
	</UserControl.Resources>
	
	<DockPanel>
		<ToolBar DockPanel.Dock="Top">
			<Button Content="Run" Command="{Binding RunCommand}"/>
			<Button Content="Save as Scenario" Command="{Binding SaveAsCommand}"/>
			<Separator/>
			<Button Content="Close" Command="{Binding CloseCommand}"/>
		</ToolBar>
		<ContentControl Content="{Binding Source}" ContentTemplateSelector="{StaticResource templateSelector}" DockPanel.Dock="Top" />
		<TextBlock Text="{Binding Result}" />
	</DockPanel>
</UserControl>
