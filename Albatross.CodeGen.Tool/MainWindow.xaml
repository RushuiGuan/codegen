<Window x:Class="Albatross.CodeGen.Tool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:vm="clr-namespace:Albatross.CodeGen.Tool.ViewModel"
		xmlns:local="clr-namespace:Albatross.CodeGen.Tool.View" 
        mc:Ignorable="d"
        Title="Albatross Code Generator" Height="350" Width="800" Padding="0" Margin="0" FontSize="12">

	<Window.Resources>
		<ResourceDictionary Source="/View/Style.xaml"/>
	</Window.Resources>

	<DockPanel>
		<local:NavigationView DockPanel.Dock="Left" DataContext="{Binding Nav}">
		</local:NavigationView>
		
		<Border Padding="5">
			<TabControl DataContext="{Binding ShellViewModel}" ItemContainerStyle="{StaticResource tab}" x:Name="tabs" SelectedItem="{Binding Current, Mode=TwoWay}" />
		</Border>
	</DockPanel>
</Window>
