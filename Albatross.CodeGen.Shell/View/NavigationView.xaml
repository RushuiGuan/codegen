<UserControl x:Class="Albatross.CodeGen.Shell.View.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Albatross.CodeGen.Shell.View"
			 xmlns:vm="clr-namespace:Albatross.CodeGen.Shell.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

	<Border BorderBrush="Silver" BorderThickness="1 0 1 0">
		<StackPanel Orientation="Vertical">
			<TextBlock Margin="5"><Hyperlink Command="{Binding CodeGeneratorsCommand}">Code Generators</Hyperlink></TextBlock>
			<TextBlock Margin="5"><Hyperlink Command="{Binding CompositesCommand}">Composites</Hyperlink></TextBlock>
			<TextBlock Margin="5"><Hyperlink Command="{Binding ScenariosCommand}">Scenarios</Hyperlink></TextBlock>
			<Separator/>
			<TextBlock Margin="5"><Hyperlink Command="{Binding AssemblyLocationsCommand}">Assembly Locations</Hyperlink></TextBlock>
		</StackPanel>
	</Border>
</UserControl>
