<UserControl x:Class="Albatross.CodeGen.Shell.View.TableInputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Albatross.CodeGen.Shell.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<UserControl.Resources>
		<ResourceDictionary Source="Style.xaml"/>
	</UserControl.Resources>
	
	<StackPanel>
		<GroupBox Header="Table">
			<Grid Style="{StaticResource inputLayout}">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="100"/>
					<ColumnDefinition/>
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition/>
					<RowDefinition/>
				</Grid.RowDefinitions>
				<Label Style="{StaticResource label}">Schema</Label>
				<TextBox Style="{StaticResource textInput}" Grid.Column="1" Text="{Binding Schema}"/>

				<Label Grid.Row="1" Style="{StaticResource label}">Name</Label>
				<TextBox Grid.Row="1" Style="{StaticResource textInput}" Grid.Column="1" Text="{Binding Name}"/>
			</Grid>
		</GroupBox>
		<local:ServerInputView DataContext="{Binding Server}" />
	</StackPanel>
</UserControl>
