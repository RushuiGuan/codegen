<Window x:Class="Albatross.CodeGen.Shell.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:vm="clr-namespace:Albatross.CodeGen.Shell.ViewModel"
		xmlns:local="clr-namespace:Albatross.CodeGen.Shell.View" 
        mc:Ignorable="d"
        Title="Albatross Code Generator" Height="350" Width="800" Padding="0" Margin="0" FontSize="12">

	<Window.Resources>
		<ResourceDictionary Source="/View/Style.xaml"/>
	</Window.Resources>

	<DockPanel>
		<Menu DockPanel.Dock="Top" FontSize="14">
			<MenuItem Header="File" Style="{StaticResource menuItem}">
				<MenuItem Header="Code Generators" Command="{Binding CodeGeneratorsCommand}" />
				<MenuItem Header="Exit"/>
			</MenuItem>
			<MenuItem Header="Settings">
			</MenuItem>
		</Menu>
		<Border Padding="5">
			<TabControl ItemContainerStyle="{StaticResource tab}" x:Name="tabs" />
		</Border>
	</DockPanel>
</Window>
